# Netlify deployment for TanStack Start.
# The official TanStack Start Netlify adapter produces client assets in dist/client
# and server-side code as Netlify Functions.

[build]
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "22"
